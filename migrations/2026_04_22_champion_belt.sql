-- ============================================================================
-- Привилегия Чемпиона (God Mode в Лидерборде)
-- ----------------------------------------------------------------------------
-- Добавляем users.has_champion_belt — флаг действующего чемпиона.
-- Если у пользователя has_champion_belt = true, он видит скрытые (show_revenue
-- = false) кассы других бойцов в «инсайд»-режиме: полупрозрачная фиолетовая
-- подпись с иконкой 👁️ и соответствующий тост при входе в рейтинг.
--
-- По умолчанию флаг выключен у всех (default false). Включать вручную
-- победителю/чемпиону (например, ежемесячному лидеру P4P).
--
-- Примени это в Supabase SQL Editor один раз.
-- ============================================================================

alter table public.users
    add column if not exists has_champion_belt boolean not null default false;

-- (опционально) — выдать пояс конкретному tg_id:
-- update public.users set has_champion_belt = true where tg_id = <TG_ID>;
